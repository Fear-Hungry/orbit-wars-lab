# Producer Opponent Fixture

This fixture vendors Slawek Biel's public "The Producer Agent" Kaggle notebook output for local Orbit Wars validation.

It is intentionally tracked because `producer` is the default benchmark opponent. Do not treat this directory as disposable artifact output.

To refresh it explicitly, run:

```bash
rtk .venv/bin/python -m scripts.prepare_producer_opponent
```

